function uniteUnique(...restOfArrays){
const oneArray = restOfArrays.flat()
 const uniqueNumbers = []
 for(let num of oneArray){
  if(!uniqueNumbers.includes(num)){
    uniqueNumbers.push(num)
  }
 }


return uniqueNumbers 


}
uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1])
