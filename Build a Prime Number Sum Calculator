function sumPrimes(n){
// if the number is less than two no primes exists
if (n<2) return 0;

//this function checks if a number is a prime

function isPrime(num){
  if (num<=1) return false;

// Only check divisors up to √num
for(let i = 2; i<= Math.sqrt(num); i++){
 if(num % i === 0) return false;
}
return true;
}
let sum = 0;

for (let i = 2; i <= n; i++){
  if (isPrime (i)){
    sum +=i; //Add prime number to the sum
  }
}
  return sum; //
}



console.log(sumPrimes(10)) 
