function bouncer(arr){
  // remember jS already has an built in funtion to remove all falsy values.
  return arr.filter(Boolean)
}

 /* let truefyResult = [];
  //console.log(bouncerArr)

for (let i=0; i<arr.length; i++){
  
 let value = arr[i]
// remember that the not operator (!)  and the "AND" (&&) operator

// also remember that NaN is not equall to NaN
  if (value !== false && value !== null && value !== 0 && value !== "" && value !== undefined  &&  !Number.isNaN(value)){
   
    truefyResult.push(value);

  }

}

return truefyResult
} */

console.log(bouncer([7, "ate", "", false, 9]))
console.log(bouncer([false, null, 0, NaN, undefined, ""]))
